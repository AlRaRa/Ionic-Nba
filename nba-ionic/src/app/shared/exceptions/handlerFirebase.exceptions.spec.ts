import { HandlerFirebaseExceptions } from './handlerFirebase.exceptions';

/*describe('Handler.Exceptions', () => {
  it('should create an instance', () => {
    expect(new HandlerFirebaseExceptions()).toBeTruthy();
  });
});*/
